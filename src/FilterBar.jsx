import React from 'react';

class FilterBar extends React.Component {
  constructor(props) {
    super(props);
  }
  render() {
    return (
      <p>Filter Bar</p>
    );
  }
}

export default FilterBar;